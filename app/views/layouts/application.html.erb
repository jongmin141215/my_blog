<!DOCTYPE html>
<html>
<head>
  <title>MyBlog</title>
  <%= stylesheet_link_tag 'articles', media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav id="navbar">
    <section class="menu">
      <% if logged_in? %>
        <%= link_to 'log out', root_path, method: :delete, class: 'menu-link' %>
      <% else %>
      <% end %>
    </section>
  </nav>
 <div class="message"><br>
  <%= flash[:alert] %>
  <%= flash[:danger] %>
  </div>
  <%= yield %>
  <footer>
    <p>
      <%= link_to 'About', '#', class: 'footer-el' %>
      <%= link_to 'Contact', '#', class: 'footer-el' %> 
      @2015 Jongmin's Travel Blog
    </p>
  </footer>
  <%= debug(params) if Rails.env.development? %>
</body>
</html>
